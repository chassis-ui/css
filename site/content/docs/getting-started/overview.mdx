---
title: Getting Started
description: Install Chassis CSS, set up the build, and get oriented — from a CDN quick start through bundler setup to a full fork-and-own workflow.
toc: false
aliases:
  - "/getting-started"
  - "[[docsref:/getting-started]]"
sections:
  - title: What is Chassis?
    description: A design-system-first CSS framework that keeps Figma and code in sync — and what makes it different from a typical stylesheet.
  - title: Quick Start
    description: Load the compiled CSS from a CDN into a single HTML file and try Chassis in a browser — no npm, no bundler, no configuration.
  - title: Installation
    description: Install `@chassis-ui/css` and its `@chassis-ui/tokens` peer with pnpm or npm. Covers the import order, settings overrides, and what the package ships.
  - title: Clone and Customize
    description: For teams who want to own the framework — clone Chassis, point it at your own token build, and ship a CSS bundle that's entirely yours.
  - title: Webpack
    description: Bundle Chassis CSS and JS in a Webpack project. Covers loaders, the import order, and CSS extraction for production.
  - title: Parcel
    description: Use Chassis with Parcel's zero-config bundler. Sass detection, project scaffolding, and the import path that matters.
  - title: Vite
    description: Set up Chassis CSS in a Vite project. Sass preprocessor config, Vite setup, and token resolution.
  - title: JavaScript
    description: How Chassis JS plugins work — data attributes, the programmatic API, events, and the real list of plugins shipped in the package.
  - title: Browsers & Devices
    description: Supported browsers and platforms, plus a few mobile-specific behaviours worth knowing about.
  - title: Contribute
    description: Set up the project locally with pnpm and Astro, build the docs, and run the test suite.
---

## What's in this section

This section gets you from "I've heard of Chassis" to a working build pointing at your own design tokens. It's organized as a path: each page assumes you've read the ones before it.

You don't need to read everything to start using Chassis. If you're evaluating it, [What is Chassis?](#what-is-chassis) and [Quick Start](#quick-start) are enough. If you're ready to install, jump to [Installation](#installation). If your team will own the framework, [Clone and Customize](#clone-and-customize) is where you'll spend most of your time.

## Where to start

The pages have a deliberate order:

1. **[What is Chassis CSS?]([[docsref:/getting-started/what-is-chassis]])** — The conceptual frame: Chassis isn't a stylesheet, it's a system rooted in Chassis Tokens. Read this first to understand the rest of the docs.
2. **[Quick Start]([[docsref:/getting-started/quick-start]])** — Load the compiled CSS from a CDN and see Chassis render in a browser. No npm, no build step. Use it to evaluate Chassis or prototype before investing in a build.
3. **[Installation]([[docsref:/getting-started/installation]])** — The real setup: install `@chassis-ui/css` from npm, install the `@chassis-ui/tokens` peer, and import them in the right order. This is the page most projects actually need.
4. **[Clone and Customize]([[docsref:/getting-started/clone-customize]])** — For teams building a long-lived design system. Clone the framework, swap in your own token build, configure settings, and ship a CSS bundle that belongs to your product.
5. **[Webpack]([[docsref:/getting-started/webpack]])**, **[Parcel]([[docsref:/getting-started/parcel]])**, **[Vite]([[docsref:/getting-started/vite]])** — Bundler-specific recipes once you know the import order. Pick the one that matches your project.

The remaining pages are reference material — read them when you need them.

- **[JavaScript]([[docsref:/getting-started/javascript]])** — Data attributes, the programmatic API, events, and the full list of plugins.
- **[Accessibility]([[docsref:/core-concepts/accessibility]])** — What Chassis gives you for free, what's still your responsibility.
- **[Browsers & Devices]([[docsref:/getting-started/browsers-devices]])** — Supported browsers and known mobile quirks.
- **[RTL]([[docsref:/core-concepts/rtl]])** — Right-to-left support and how to enable it.
- **[Contribute]([[docsref:/getting-started/contribute]])** — Local setup for working on Chassis itself.

## How Chassis differs

Most CSS frameworks are a single stylesheet you drop into a `<link>` tag, possibly with a Sass entry point and a small set of override variables. Chassis adds two things on top of that:

- **Tokens are a separate package.** Every visual value — colors, spacing, typography, motion — comes from `@chassis-ui/tokens`, which Chassis CSS imports as a peer dependency. Replace that package with your own token build and the same Chassis CSS code becomes your design system.
- **The framework is meant to be cloned.** For long-running products, the recommended path isn't to install Chassis as an npm dependency and override variables. It's to fork the repository, point the `_vendor.scss` import at your own tokens, and own the CSS build. Chassis is designed to disappear into your design system, not sit on top of it as a dependency.

Both paths are documented. Quick Start and Installation cover the dependency path; Clone and Customize covers the fork path. Pick the one that fits the lifespan of what you're building.
