---
title: Object fit
description: Responsive `object-fit` utilities for controlling how replaced element content fills its container.
toc: true
css_layer: utilities
---

import { getData } from '@libs/data'
export const bgColor = Object.fromEntries(getData('context-colors').map((c) => [c.name, c.hex])).neutral
export const fgColor = "#fff"

## Overview

`object-fit` utilities set the CSS `object-fit` property on replaced elements such as `<img>` and `<video>`, controlling how the content is scaled to fit the container. Available values:

- `contain`
- `cover`
- `fill`
- `scale` (for scale-down)
- `none`

## Examples

The `object-fit-{value}` class is applied directly to the replaced element:

<Example class="d-flex overflow-auto" code={`<Placeholder width="140" height="120" class="object-fit-contain border rounded" text="Object fit contain" background="${bgColor}" color="${fgColor}" markup="img" />
<Placeholder width="140" height="120" class="object-fit-cover border rounded" text="Object fit cover" background="${bgColor}" color="${fgColor}" markup="img" />
<Placeholder width="140" height="120" class="object-fit-fill border rounded" text="Object fit fill" background="${bgColor}" color="${fgColor}" markup="img" />
<Placeholder width="140" height="120" class="object-fit-scale border rounded" text="Object fit scale down" background="${bgColor}" color="${fgColor}" markup="img" />
<Placeholder width="140" height="120" class="object-fit-none border rounded" text="Object fit none" background="${bgColor}" color="${fgColor}" markup="img" />`} />

## Responsive

Responsive variants follow the format `.{breakpoint}:object-fit-{value}` and activate at the specified breakpoint and above:

<Example class="d-flex overflow-auto" code={`<Placeholder width="140" height="80" class="small:object-fit-contain border rounded" text="Contain on small" background="${bgColor}" color="${fgColor}" markup="img" />
<Placeholder width="140" height="80" class="medium:object-fit-contain border rounded" text="Contain on medium" background="${bgColor}" color="${fgColor}" markup="img" />
<Placeholder width="140" height="80" class="large:object-fit-contain border rounded" text="Contain on large" background="${bgColor}" color="${fgColor}" markup="img" />
<Placeholder width="140" height="80" class="xlarge:object-fit-contain border rounded" text="Contain on xlarge" background="${bgColor}" color="${fgColor}" markup="img" />
<Placeholder width="140" height="80" class="2xlarge:object-fit-contain border rounded" text="Contain on 2xlarge" background="${bgColor}" color="${fgColor}" markup="img" />`} />

## Video

The same utilities apply to `<video>` elements:

```html
<video src="..." class="object-fit-contain" autoplay></video>
<video src="..." class="object-fit-cover" autoplay></video>
<video src="..." class="object-fit-fill" autoplay></video>
<video src="..." class="object-fit-scale" autoplay></video>
<video src="..." class="object-fit-none" autoplay></video>
```

## CSS

Object fit utilities are generated via the utilities API.

### Reference

The table lists all object fit utilities and their CSS properties.

<UtilityReferenceTable utility="object-fit-" />

### Utilities API

Object fit utilities are declared in `scss/utilities/_index.scss` using the [utilities API]([[docsref:/utilities/api#using-the-api]]).

<ScssDocs name="utils-object-fit" file="scss/utilities/_index.scss" />
