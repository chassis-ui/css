---
title: Vertical alignment
description: Vertical alignment utilities targeting the CSS `vertical-align` property for inline and table-cell elements.
css_layer: utilities
---

Vertical alignment utilities set the CSS `vertical-align` property. They apply only to inline, inline-block, inline-table, and table-cell elements. For vertically centering block content, see the [flex utilities]([[docsref:/utilities/flex#align-items]]).

Choose from `.align-baseline`, `.align-top`, `.align-middle`, `.align-bottom`, `.align-text-bottom`, and `.align-text-top` as needed.

Applied to inline elements:

<Example code={`<span class="align-baseline">baseline</span>
<span class="align-top">top</span>
<span class="align-middle">middle</span>
<span class="align-bottom">bottom</span>
<span class="align-text-top">text-top</span>
<span class="align-text-bottom">text-bottom</span>`} />

Applied to table cells:

<Example code={`<table style="height: 100px;">
    <tbody>
      <tr>
        <td class="align-baseline">baseline</td>
        <td class="align-top">top</td>
        <td class="align-middle">middle</td>
        <td class="align-bottom">bottom</td>
        <td class="align-text-top">text-top</td>
        <td class="align-text-bottom">text-bottom</td>
      </tr>
    </tbody>
  </table>`} />

## CSS

Vertical align utilities are generated via the utilities API.

### Reference

The table lists all vertical align utilities and their CSS properties.

<UtilityReferenceTable utility={["align-baseline", "align-top", "align-middle", "align-bottom", "align-text-"]} />

### Utilities API

Vertical align utilities are declared in `scss/utilities/_index.scss` using the [utilities API]([[docsref:/utilities/api#using-the-api]]).

<ScssDocs name="utils-vertical-align" file="scss/utilities/_index.scss" />
