[
  {
    "class": "d-none",
    "reason": "js/src/nav-overflow.ts toggles this class via classList.toggle('d-none', ...); Tailwind's own class-scanner never sees it in any source file, so it needs an explicit @source inline() safelist entry to be generated at all."
  }
]
